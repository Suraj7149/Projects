#include<stdio.h>

void main(){
    // Arithmetic Operator:- +, -, * for multiplication, /, % for Modulus
    
    int a,b;
    a = 30;
    b = 6;

    printf("a + b = %d\n",(a + b));
    printf("a - b = %d\n",(a - b));
    printf("a x b = %d\n",(a*b));
    printf("a / b = %d\n",(a / b));
    printf("a %% b = %d\n",(a % b));
    
    // Relational Operator
    // Logical Operator
    // Bitwise Operator
    // Assigment Operator


}